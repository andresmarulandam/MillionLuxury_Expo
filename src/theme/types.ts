export type ThemeType = 'light' | 'dark';

export interface AppTheme {
  background: string;
  card: string;
  text: string;
  textSecondary: string;
  primary: string;
  error: string;
  success: string;
  border: string;
}
